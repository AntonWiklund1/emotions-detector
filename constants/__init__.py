image_size = 224
patch_size = 16
num_classes = 1000
embed_dim = 768
T = 2  # Temperature for distillation
lambda_coeff = 0.5  # Coefficient for distillation loss
num_epochs = 50  # Number of epochs for training
batch_size = 64  # Batch size for training